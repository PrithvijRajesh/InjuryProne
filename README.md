# InjuryProne

Interactive body model for pinpointing muscle soreness/injury. See `prd.md` for the product spec, `WorkLog.md` for milestone history, and `FeatureAnalysis.md` for how each feature works.

## Development

```
npm install
npm run dev
```
